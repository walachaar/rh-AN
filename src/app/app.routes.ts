import { Routes } from '@angular/router';
import { WorkerPageComponent } from './components/worker-page/worker-page.component';

export const routes: Routes = [{ path:'',component: WorkerPageComponent}];
